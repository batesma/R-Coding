# CLEAN UP #################################################
# Clear environment
rm(list = ls())
# Clear packages
detach("package:datasets", unload = TRUE)  # For base
# Clear plots
dev.off()  # But only if there IS a plot
# Clear console
cat("\014")  # ctrl+L
# Clear mind :)
install.packages('dr4pl',
dependencies=TRUE,
repos='http://cran.rstudio.com/')
library(dr4pl)
#Install Packages
install.packages("Rtools")
install.packages("rtools")
install.packages(c("caret", "cli", "colorspace", "curl", "dplyr", "fansi", "haven", "htmltools", "isoband", "jpeg", "lme4", "maptools", "mime", "openssl", "openxlsx", "pROC", "quantreg", "Rcpp", "readr", "roxygen2", "stringi", "tibble", "tzdb", "utf8", "vroom", "xfun", "zip"))
